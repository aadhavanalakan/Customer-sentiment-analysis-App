# Direct dependencies (pip resolves the rest).
# For an exact, reproducible install use uv:  uv sync  (see uv.lock)
streamlit>=1.58.0
streamlit-option-menu>=0.4.0
pandas>=2.2
numpy>=1.26
plotly>=6.8.0
textblob>=0.20.0
wordcloud>=1.9.6
matplotlib>=3.8
